commands helper
brew service start postgresql@15


Online/offline indicator

-> Can have a DB That have userId and isOnline field
-> userID -> isOnline [key -> value] access pattern, DB’s like redis ,DynamoDB can be used
-> Can have a batch API to get status of multiple users
-> How the writes would happen as user comes marks it as online and as he goes post request comes to mark as offline but there could be 
   unexpected drop-offs where there is power failures at users end so better is that from user we get heartbeats after a certain interval 
-> so in DB better is we can have userId-> lastHearbeatTime
-> those having lastheartbeat time < current - interval would be considered offline
-> or we can have ttl for 30s[interval] on heartbeat so who so ever userId is not in list is expired
-> for hearbeat write on scale there is a lot of writes for this we can use Connection pooling
-> As the service starts establish some persistant connection[raw on TCP] with database that would prevent establishing a tcp connection creation/removal for just a field update in DB
-> suppose there are 10 service instances and db concurrent connections supported are 100 now we set some minimum connection according to which as soon as instance 
   starts will establish connection and as per the no of requests received connections are made and added to connection pool as soon as max limit reached no new connection
   is made it will wait for previous connection to get free for a particular instance and suppose a connection is not used from a lot of time so it will expire and when needed
   a new connection can be created again by may be for some different instance or same one.or explore other ways to do connection pooling also.

SO in this system bottleneck for scale is number of connections not the storage

Connection pooling
-> maximumPoolSize per server
maximum connection one instance can have at a time with DB
suppose DB supports 100 connection general rule is 0.6*{maxconnection}/numberOfservers

-> minimumIdle
minimum no. of connections per instance created at the time of initialisation
generally 20% of max number of connections

-> connectionTimeOut
time for which a request waits for a connection and gives exception if pool is exhausted
2000ms - for low latency system
5sec - for batch jobs

-> maxLifeTime
Max life of a connection before it is recycled.
generally 30 min

-> idleTimeout
remove the connections from pool that are idle but maintain minimuIdle
Spiky traffic → keep higher (1–5 min)
Stable traffic → keep lower (30–60 sec)

So as soon as service starts minimum connections are established as per requests more connections are added to the pool with limit maximum pool size and on connection timeout request
throw Error applying backpressure, maxLifeTime recycle the connections before DB server timeout and the connections that are idle for a while are removed as per idleTimeout config.

Best library - HikariCP 



Database insights
-> suppose we are building a blog app
-> now it would have
users
-> id
-> name
-> bio
blogs
-> id
-> is_deleted: for soft deletes so that we can have it recoverable and also performace inc. as Btree node in database don't have to rebalance which happens on delete.
-> author_id
-> published_at: for redabilty store in string as date: time but, it is not space efficient + if we query for some comparision it is not efficient as we have to convert it into 
   date,month year and time than compare, db's have some format dateTime in that we can also store but could be inefficient in some ways
   [every db has it's own implementation but all provide this dateTime], we can store in epoch but it would reduce redability, best is we can store int as yyyymmdd 
   this way we can compare + space efficient.
-> title
-> body: this we would have as LONG_TEXT not VARCHAR as in long text referance of the field would be stored in column not the field value, it have 2 disc reads one to get row then 
    one to get column[body] value which is compute intensive but it is good as suppose in this case we just want list of blogs then in that we just get these rows from which 
    we can read author and published_at no need for body once user clicks on a particular item from list then we get that row only with it's body.

caching
Anything that saves discIO, networkIO and computation is a cache
question: should we compress cache and is there any cache that provides compression on storage and decompression while returning
ans: yes we can have cache with compression but out of the box cache should not provide that as we save on storage while compressing data but while passing decompressed data
we are making a bad networkIO opp so better is we should keep the decompression logic at client end.


question:
if we store cache on disk[db] or on redis how to decide
suppose we have persistant connection so network IO is nullified
redis(would store in ram)
-> just key value we can get instantly
-> for small reads redis would be way faster as time spent on read is very small when reads are big there is little diff only.
in DB
-> have to traverse a tree to get to particular row and giv result (logN)
-> we can store huge data in it

TODO:
for this we can read more about redis and DynamoDB
read how sql stores data internally
5 possible ways that you can cache with a example

cache
-> on application server RAM : suppose if we have a nearly static data where inconsistancy doesn't matter or request from a user can only reach a application server via some
load balancer or routing technique so this will be effective
: it will have limited space and if server goes down it will be removed
-> material views: where data are joined or some new column is added with some precompute if most of the queries are onto this type of data we can make a view of it
: challenges that comes with it is triggering update of views on db update mostly used with not so much changing data
-> browser
-> CDN
-> Disk of API server: it is having OS and some packages ram is there that we have chached on in 1st point but it is limited  but disk is majorly unused we can utilize that
if some inconsistency is fine
-> load balancers 
-> centalized remote cache: redis provides consistency

scaling
vertical scaling is possible upto a extent then horizontal scaling comes into picture
we can have more API servers but for them to support we have to scale upstream that is DB servers.

majorly load is read load so we can add read repicas
if write load is there then we can do sharding.

deligation
whatever is needs to be done in real time only do that rest deligate.

cuncurrency - message queues, message streams

comunication - 
-> short polling: when client polls the server for status
-> long polling: when client calls but server sends the response once job is completed. then client again calls that just reduce no. of calls
rest specifications, http requests
websockets- socket.IO
server sent events - which are persistant unidirectional events like stock APP (implementation on MDN)




Relational databases

-> passimistic locking : insure consistency in case of concurrent transactions
deadlocks: generally databases try to avoid deadlocks by running a deadlock detection algorithm before any transaction could accuire lock [to avoid lock transaction kills itself]

shared lock 
Select * from ... for shared
-> other transactions can have shared lock on same rows
T1 -> row 1,2,3: read and modify but for modify it has to be converted to exclusive lock for that all other threads have to release there shared lock-- not sure on this check it
T2 -> row 3,4,5 : read
read throuput increases.

exclusive
Select * from ... for Update
untill T1 is complete T2 can't even read.

skip lock
it's just a extension
Select * from ... for Update skip locked
it will skip all those rows that are already locked by some other thread but get lock on rest

NOWAIT
Select * from ... for Update NOWAIT
if any row is already locked kill self transaction

So if we have isolation levels in ACID then why this locking is needed as those isolation level are on transactional level which imapact scheduling of transaction like serializable
would not allow other transaction to run[in this case extra locking is not needed] which most of system won't have because of high throughput needs other repeatble read, read commit
read uncommit these don't block tranactions to access same row so they have edgecases which could leed to wrong/stale reads in tranctions which is fine with ACID as we chose that
that isolation level it is Consistent at db level but from application perspective we have got wrong data that's why this locking mechanism is important.


suppose if a query is fired for concurrect transactions[120] which looks for first row[120 ] column c1 that is null and update that to some value what will happen
all the threads would get 1 row see it as null and update that but few threads that are scheduled late will might see 1 row as non-null and update second

suppose if we employ exclusive lock here in query
now all try for 1st row one thread would get it other go into some wait state[means in wait queue] then once commit happen all the other thread are signaled to re-execute the query 
again then all the rows would be filled sequencially but by any thread who so ever got lock. it will cause n*(n+1)/2 operations in db

if we add skip lock
we would see significant time difference, all the rows would get filled, but if some transaction fail and others moved ahead of that row due to skip it will be unfilled, as there
is no waiting in queue, query would be completed for rest transactions. it would just cause n operations as db would do one disk read means read a node in b tree then that contains 
some rows it will lock 1 for 1 transaction and give other row to another so no extra opp required for other transaction.

what ever we use out of these is the gauranty that our db provide, relational db has given us all the posibilities 

so rule of thumb is fixed inventry[rows]+fixed contention[threads trying for writes]= use locking

If we have tried with shared lock
all threads accuired shared lock all waiting on each other to convert to exclusive to write...Deadlock!!

setup
autocommit off: as moajorly db keeps transaction as isolated so if transactionStart[select statement with lock] gets commited it would cause lock release, but we want to update and
release because then other transactions can see value in that field and move ahead. in realworld also the query that is wrapped inside Transation db's generally
set the commit to off for the time of execution.

transactions starts

do some work so add some sleep: in real world the transactions take some time to compute as this is small transaction so even if we apply locking or not it will have same effective
if we don't add sleep as the second transaction comes would have already completed so we can't able to see the lock effect that we generally see in real world.

transaction commit

1:26

Key value store from relational database
thinking on optimal ways and query's for them
primary key
datatype of cols
want to have expire -indexing what 
delete opp- soft delete utilizing expiry cols set -1 in expiry[micro optimisation delete only if not expired]
put- using upsert[doing it at db layer] + saving get at application layer
get -  always with expire condition[Select * from store where key =k1 and ttl > now()][this always saves networkIO but to save on disk IO when index is on[key,ttl] 
else whole table is read which doesn't save disk IO]
TTL- what query is optimal [to check expiry at db layer]

then delete in batches to have minimum rebalancing
2:06

Delete approach
for disk: we can run a periodic job to delete all the expired keys

only for in memory database[redis]
As the delete can be pointed no need to rebalance the Btree just freeing up the memory, so what we can do is whenever some key is accessed that is expired delete that key[lazy deletion].
this does 90% of job rest 10% is done by periodic job, but this is not that complete deletion job as redis is single threaded[don't have alot of time] it employs a algorithm
gets only random set of 20 keys finds the expired and delete them if they are more than 25% rerun the deletion else just leaves this is fast + solves the problem for memory.


Architechure
LB-> KV pair API server -> db[sharded with replicas]
try to implement from kv server to sharded db with connection pooling
try to spin up kafka and rabbit mq and read messages for them
learning
-> Replication in PostgreSQL is primarily log-based. The primary writes changes to WAL, which is streamed to replicas. Replicas apply those logs to stay in sync.
Depending on configuration, replication can be synchronous or asynchronous, trading off between consistency and latency.

steps 
replica establishes a connection with primary and tells I have logs untill this point send afterwards
Your Java app sends a POST request to insert "apple".
The Primary database writes "apple" to its own local disk.
The Primary database immediately replies to your Java app with a "Success" message.
A fraction of a second later, the Primary drops that unread log data onto the streaming connection, sending it to the Replica.


Non Relational databases
Document databases:
-> like mongodb, elasticsearch
-> closest to relational
-> json based
-> supports complex queries like in json get all the documents having this value for this key
-> but can't partitian more if having such queries as these queries are cross shrad and would cause a lot of time
-> cross shard means for a same request we need to look into 2 shards or 2 db servers
-> more partiatian provides more throughput

key value 
-> redis, Aerospike, DynamoDB
-> key wise access pattern restrict cross shard queries 
-> can be heavily partitianed

column oriented databases
-> like relational one are row oriented column oriented we can visualize as all the different columns are stored in diff files majorly fit in analitics usecase like avg for particular
column. majorly datawarehouses use them[where we store full data log like whole history]
-> insertion would be heavy
-> columns are homogeneous data so can be compressed heavly so provide good space optimisation
paper: c-store a column oriented database

Graph database
-> neo4j,neptune, Dgraph
like it stores data in adgecency list
-> when we want to use graph algorithms
-> like fraud detection[1 transation not like you usual one], recommendation system[if you lie in a cohort you would be recommended]
-> like linkedin 1st, 2nd connection


relational 
when we want strong consistency, foreign key and all, fixed schema we go for relational
non-relational 
when we no constraints that relational provides we go for non relational which we can leaverage for sharding the data 

why sharding is a bit risky in relational sometimes[pointing out just one case]{this is for db level sharding}
{In sharding we can choose which table to shard or not generally smaller tables are kept in all the shards, or sometimes tables that are joined frequently are sharded with same 
key, while marjorly we don't shard on diff keys but can be a usecase then we need to do some things to avoid cross shard join discussed down a case}
-> case suppose we have 2 tables in our db both are sharded suppose one has key of other which is foreign to table A and primary to table B
-> if sharded on same key then there data would be present in same shard so contraints of foreign key would be check that table B should have row for that key before adding to tableA
-> if sharded on diff keys then to check foreign key constaints cross shard network IO will happen which cause latency
-> what we can do: 
   -> enforce integrity at application layer and drop foreign key contraint
   -> denormalization: add colums in table A that you want to use from B via foreign key and drop containt
   -> async validation drop containt

{if we do application level sharding then the referential integrity should also be taken care at application layer}

1:00

slack design
DB schema
users: id, name
channels: id, org, name,type[dm/grp/channel]
membership: userId,channelId, readUpto <- this membership we can use to have all user and channel specific relation
messages: from, channelId, createdAt, msg

As there would be a lot of messages so sharded db is required we can go with casandra or shard sql database by ourselves

for even 2 person we can have channel on UI it will be rendered like DM but on backend it is channel as this design is scalable otherwise for message we have to have channelId or 
userId of the receiver and if message is for channel then that receiverId would be null and if for receiver than channelId would be null and for retrival we need to write a logic
that which type of message is it than have to retrieve it so better we have all type of conversations as channels only.

HLD
with different gurantees we can have a lot of possible designs 
Approach 1
users have a websocket connection[majorly for receiving messages]
user1 [sends message in channel] -> LB -> API servers -> Db
API server(after persisting the message)-> Websocket server -> forwards to other connected users
gurantee: message persistance as in slack type system it is a good thing to have
why we are not just doing this on WB server as it is a security problem If we directly make connection with wb and db, and also if we put some heavy logic on wb,
if it goes down whole realtime communication can go down as for normal API servers users are not connected to them but WB have users connected to them.

Approach 2 
users have websocket connection for both sending and receiving messages
user1 [sends message in channel]-> websocket -> publishes in queue for async injection into DB
websocket -> sends to other receiving users
gurantee: message persistance + low letency [both mid]
this is for whatsapp like system as persistance at backend can wait as user mobile device persistance is already present

Approach 3
users have websocket connection for both sending and receiving messages
user1 [sends message in channel]-> websocket -> sends to other receiving users
gurantee: low latency
for zoom chat like system where persistance is not important

websocket imps
one thing to remember is for chrome per domain there is 6 TCP connections allowed so we can't have these persistant websocket connections for everything because suppose for our chat
system we had one then for some images getting we had another etc. then for doing other opps like click of a button trigger something would be blocked so usually we have 1 websocket
connection we do all the receiving work through that only.

websocket scale
suppose websocket server have limit 4 connections 
if we implement websocket using socketIO -> websocket know who all is connected to it like user A,B,C,D
and as they connect they pass on membership info to websocket that it stores in some cache or in disk
like A is part of -> channel c1,c2,c3
now when A forwards message it gets deliverd to all users subscribed to these channels 
but like websocket have 4 connection so other subscribers of channel1 would be on other websocket server so how would they receive, for that websocket 1 should be connected to
other websocket servers like star topology but then most of the connections of websocket is exhausted by connecting with each other which is bad 

scale: all the websocket connected to a realtime pub/sub like redis so connections would drop to 1 and realtime pub/sub doesn't provide any persistance it won't store message
it will send it to wb server subscribed to it at the moment,
like suppose A user is subscribed to slack channel 1 when it connects to websocket server, web socket server which is connected to pubsub passes info that it is subscribed to channel
which is correspionding to channel 1 of slack actually redis also has channels internally now like this whicherver server is connected to channel 1 over pubsub can exchange messages
pubsub would directly pushes the messages to them which are subscribed to that particular channel. 

so generally websocket server can handle a lot of connections so who all are subscribed to a particular channel are generally on same websocket but sometimes when there is a 
rebalancing or websocket server crash the clients connects to different servers then this pubsub is used for exchanging messages.

Security
untill now we have exposed websocket server over the internet but that can invite ddos attack but we can have ratelimiter and insure safety practices[as we are not directly 
calling db also] if we want to have it as realtime, but if we can afford some latency we can put it behind load balancer and infact we are getting membership info from client
instead we can have it directly from db or through some API call.

Distributed systems

It is all about scalling system horizontally

Load balancer
By round robin:
if the request are homogeneous[means will take about same time to execute] and infra is also homogeneous[means having equal hardware capacity]
simply distributing load via round robin is good.

By weighted round robin:
if suppose 1 API server is having more ram than others then it should be requested more

Least connections 
pick the server that is having least active connections.[good for hetrogenous request]

implemenation wise
just have a strategy of balancing load
according to it find the target server build a connection with it
then map request connection with that target connection and pass the incoming and outgoing message.

simple imlementation
Now as we have more than 1 backend servers load balancer will have a config containing
-> mapping: source and target connection
-> load balancing strategy
-> health check for targets

//orchestrator is multiple nodes running on standby with leader election if master goes down 
now there is a orchestrator layer in betweeen that checks health of backend servers and updates in Db because direct passing it to lb would create a different problem
as lb can be more than 1 and they might go down and new one need that info so DB would be appropriate to have this info.
DB would have
-> load balancing strategy
-> health check for targets

if LB would look this before every request it would add latency

now either Lb have to poll db for health checks so that requests are not forwarded to not active servers or this info is pushed to LB
polling
-> this happens in interval suppose if server goes down just after a poll is finished it would cause the issue that we are trying to solve.

pushing
one way to push is orchestrator updates DB and ask LB to pull data but if there are different LB's it has to be done for all, still that is fine.
other way could be as we want to push the data to multiple servers -> after orchestrator updates DB and either orchestrator pushes new data in realtime pub/sub[redis] or DB through CDC pass this 
info to pub/sub that would go to LBes.

now how would we know that we want to scale LBes or even the backend servers we would have a telemetry agent running in VM's that would pass metrics to a DB[prometheus] that 
metrics orchestrator or autoscaler can analyse and scale instances, here scaling for backend servers would be handled as orchestrator would know through metrics orchestrator or autoscaler
and puts that backend server in db,
but if LB gets added and there are multiple LB's to whome the request goes from client because client knows 1 IP to connect to,
if we have a load balancer on it that would even make the communication complex + adds latency due to another network hop

now DNS comes into picture those LB's have some IP's DNS has mapping of those IP's with the domains
if public DNS
google.com -> IP1,IP2,IP3

private DNS -> DNS core
my-service -> IP1,IP2,IP3

as the request comes first Domain name is resolved picking some LB IP with some balancing technique than passing the request to particular LB.

Remote locking
(whichever is the closest storage layer locking is done on that)
at the time of airline ticket booking system thread take locks at db level.
at process level suppose I ran same process from 2 windows it might say one is already running and exit there DISK level locking there

at machine level remote locks are the solution

lets take a example suppose there is a remote queue it just pops out a message on requesting and we can't change any impl of its because it is remote
now we have consumer but we want only one consumer to get message at a time, So to solve this we have to implements remote locks.

we have a DB that contains
Resource[on which lock is required]: C1[consumer that acquired the lock]

now before consumer gets a message from queue it has to acquire the lock means set value C1 into DB where it will be competing with other consumers.
now from DB we want key value based access paterrn
with automatic expiry and atomic opperation
Redis fits in well

so for a consumer
acquire lock 
: redis acquire
  if aquired return
  else continue checking
read message 
release lock
: check lock aquired by
  if your's release

while release one things that need to be checked is if the lock is taken by the consumer that is releasing because suppose at the time while reading if lock expired and 
some other thread took that then we are releasing someone else lock

to solve this generally EVAL is used in redis that runs LUA script which made these conditions as one statement.

Distributed locking: RedLocks[redis locks]
but here with only one redis instance it could be single point of failure so better is if we have multiple redis instance and lock is tried to be accuired on all if lock is
acquired on atleast 50% of instances it can continue to read else release of acquired one as well this insures fault tollerance and availability.

Distributed Id generators

Ids are important as they uniquely identify a entity.

for a single machine it is easy like we can take epoch time or a global counter

for more than 1 machine <machine-code>-globalcounter/epoch

but if we keep global counter and machine reboots it would again set the counter to 0

so we can store that in some disk

count = fetch()<-- from disk--only first time
giveId(){
   count++
   store count
   return <machine-code>_count
}

but it involve a lot of disk IO

we can significantly reduce that

count = fetch()+1000+1<-- from disk--only first time
store count
giveId(){
   count++
   if(count%1000 == 0){
      store count
   }
   return <machine-code>_count
}

here we are not utilizing each int but system is correct 

if we want to make it more optimize in terms of latency we can use epoch miliseconds that won't require db call at all

there could be a use case where we want monotonically inc id's across machines so that we can identify which write is latest and overwride the other
patter count be <epoch>_<machine-code> --> 1709923001
-> but as clock can't always be in sync due to heat and cold condition viberation in the physical clock of the machine can change effecting the time, so it may be in 4 machines
on same instant we get diff epoch, and when the time sync the clock that moved fast delibrately gets slow,
so if we want correctness we might have to follow that counter approach that has a bit of latency
-> on same milisecond a machine could generate 2 id's that would be same if it processed request in microsecond

As if we have a central service here that provides the id based on epoch due to load we might have a cluster of machines in this service that continuously have to check wheather
the id generated is greater than any other machine at this instant which is messy.

Use case supose we have multimaster DB we can't let it count by auto increment id otherwise there would be alot of conflicts so we have to provide custom logic for id's to it 
according to our alignments.


random uuid based approach
uuids are 16 bytes long now generally these Id's are indexed means suppose a new mapping table is created with this id and index as column than in ram it is loaded opp are performed
like join and all then finally rows are readed from the main table.
so the main part is it get's loaded to ram so we should make it size efficient to eaisly fit in ram, mongo db have {epoch,randomcounter}, 4,5,3 bytes total of 12 bytes


Amazon design [not needing monotonically inc time]
DB
service.  counter
order.     500 -> 1000
payments.  2000
DB -> Id generation service -> different services as clients

suppose order service have 5 instances one of it gets request for some id's to be provided to orders now Id generation service passes a batch of ids like 0 to 500 now when
another instance ask it provides 501 to 1000 when instance see it is running out of id's it requests and Id generation service gets it from db by having that locking and all on row
there is no collisions at all as every request is getting its own batch
downside is if client crash that batch is lost so have to finetune it in a way that batch request is not that large and loosing a batch won't impact much

How flicker does it
they need id generation service as they have sharded db and uuid don't scale

Id generation DB<- service -> DB
 there Id generation service is a DB 

DB
id[primary key]    val[unique]
count              a

which inserts a row with some dummy value every time so that unique key constraint fails unique id is formed that they just get

insert into table (val) VALUES ('a') ON duplicate KEY update id = id+1;

but as load could be more may be we need more Id generation DB to avoid single point of failure

now we can have odd values to be generated from db1 and even from db 2
we can have autoincrement to be 2 instead of 1, even if one goes down other one will provide even ids untill it recovers and once it gets back it can start from where it left or we 
can have a reset of value if we want

How twitter does it 
SNOWFLAKE
epoch[41]_machine-code[10]_counter[12]
constraints
so at max 1024 machine and 4096 request per milisecond it can provide unique Ids

these is far more superior as it doesn't require any infra or calls to db just internal liberary + can work with sharded database
and as these are roughfly sorted values byproduct is it helps while iteration of db usecase pagination

general pagination offset = x ,limit y -> offset = 2*x ,limit y

now in this DB will compute the result for 2*x and then eliminate 2*x-y it will load all the rows then eliminate y as this is made for complex query with where clause
but that query if we don't want 
with snowflake we can just 
find()+ limit(y) -> gives result we store last value(x)
find(x) + limit(y)-> O(n) +y always almost constant

so if we want last 0-100 tweets in last 5 mins we can then 100-200 we can get that with low latency and these were roughly sorted also like in the order they were twitted

2:00:00

instagram ofloads this logic of snowflake creation at service layer to DB by writing some sequencer for autoincrements so increments happens automatically with id as snowflake
this makes them more scalable as now they can have more than 2^10 machines 
there snowflake epoch[41]_db_shard_id[13]_shard_sequence_no[10]

so now they are limit by DB shards having  2^13 some 8000 shards, but that is infact very large.
BTW shards are logical seperation may be 1 postgres server have 100 shards in it


CDN
so cdn sits silently b/w backend server and user and provide benefits like serving cached content directly without contacting backend server suppose we have frontimage in our
site that we want to render that could be of 10mb of static data why we want to load our servers to provide that so we can configure CDN for this
so we have our LB domain like MYlb.co.in we configure it to cdn and cdn provides a url like MYlbCDN.co.in 
Now from our backend we provide image url as MYlbCDN.co.in/static/abc.png to out frontend when frontend renders so MYlbCDN.co.in -> resolves to cdn ip by dns where if its a 
first call request goes to our LB and cached else response is directly passed through cache, cdn provides a API to invalidate cache if we pushed a change and want to provide 
it to our users before ttl invalidates cache.

FYI: presigned URL and cdn url both have some ttl to upload and fetch

How image is uploaded
now simple way is user upload it to backend server from there it gets uploaded to S3 so suppose image is of 5mb so there are 2 network hops which increase the cost and letancy
why can't user directly push to s3 with security means as obviously we can open our S3 for public uploads

so this is done through presigned url
here as user shows intent to upload image meta data along with our s3 bucket and some randome imageId is passed to s3 via BE server[imageupload service] and a presigned url 
from our s3 bucket is fetched then frontend puts that image on that url which then generally provides CDN url in resp that is mapped to s3 as origin as cdn has integration with 
s3[with auth that we provide] to view the image we can't call the s3 path directly but with that cdn path we can call that internally fetch/cache the image and provides it.

if user want to post that image say on insta on clicking button we want to add that entry in our posts db
now what we have achived is wheather it is image upload service or reels upload service post service remains same

and for extensebility we can publish a kafka event that a new post is published that can be consumed by various consumers[analytics,feed etc.]
Posts schema
userId
caption
imageID// don't store the direct cdn path beacause that is derivable
derived path -> <cdn>/static/{userid}/{imageid}.png
suppose we want to change cdn then it's better if we have stored this way otherwise it's not possible or require huge mannul effort to convert all cdn urls

CDN as main usecase is image/video: it out of the box it provides image processing, resizeing like frontend knows user device size so in cdn url it adds query param it passes
the resized image and even caches that too.

1:50
Hashtag service
so once the user does that above post and post service publish that event to kafka with partitian key of userId because we want for a particular user events goes to same partitian
so that they are in order

what we want?
for a particular tag like #lifestyle we want no. of post published as well as some top 100 post logic of how 100 are picked we don't have to care about.

Now the post contains some tags we want to increase count of tag used for all the particular tags
so our tags db would be like (we have compiled all the necessary thing at one place so that we don't have to fetch everything from diff place to dec latency)
tagName:
tagId:
usedCount:
top100[]<-here we can have list of post Id's or denormalize this by adding actual post object

having postId provides us the updated post data but also includes processing like joing it with post db before giving it for rendering

Now for a simple system if a event comes we have to get the post extract all it's tags and then one by one update count of each tag in db that cause alot of micro writes
which is a bad design as it's unnecessaraly doing a lot of requests, as we don't want exact count also.
so better is we do batching like for a batch we update the count in db but problem is we are getting a post that has multiple tags and we have partitianed by userid for that we 
should have done by tagid but that we can't do due to the order we want as the queue provides that event to notification service also.
so what we can do is use that above queue and write a adapter with consumer that extracts tags and pass event for tag in another queue partitined by tag here we are not exactly partitianing
by exactly 1 tag each partitian because tags can be millions so we have defined some hash for tag like #life related all tags in one partitian 
Now from that partitian consumer picks event and batches it for particular tag updates count+=(n) which is optimized 
this is adapter pattern ^^

some nodes in consumer can go down we might be recounting some time but thats fine as we don't need exact count

Now we can think of how would the top100 posts come like logic some other team owns and provide the top100 post directly but how it provide
kafka again -> by sending a event when it is availble and our consumer can pick that and updates in our tags db

Now how user can see this, as request comes for read we can fetch on web server for a particular tag and show but it would most probaly be not very much updating data
so we can include a cache or a CDN with some ttl.

So more structured way to seeing this would be read and write path we go along each path with simple system then we think about how components would react in load, optimise that.

Question:
The second queue that repartitianes into hashtags 

Build notification system yt video


How Images are served
how it happens at frontend
As be provides the image url to frontend it adds that url to img tag in html and browser do a get request on it in 2xx response header it sees the type like jpeg/png and browser
has all the decoders so whever is the type it decodes the image and provides the output.

suppose image is statically present so what happens
when GET request comes to BE server req(<host>/static/fld/abc.jpeg)

Now with every framework like springboot, nginx, flask whenever there is a request comes with /static that handler sees the static folder and follow the path after that and find the 
locally present file and returns that 

Now if we want to pass a file from S3 instead of that default handler we can define our own (eg. /mystatic) and write a handler for it.

On the fly image generation
from BE to frotend a URL is provided that gets sets on opengraph <meta property="og:image" content="URL"> tag and when it hits BE the BE can produce image on the fly and return bytes
with use of some liberary and info of thing that we want to make image of, which is great for changing Images

like when we paste url of some git repo image shows the no. of contributors, forks, commits etc.


gravitar
-> it attaches your gmail id to user selected profile Icon.
-> user can upload diff images and set some image as active.
-> any app like instagram suppose if wants to pull icon can just pull it from <gravitar:host>/hash(email)

how to design this simple system
first lets think of tables that we need

users
-> userId
-> email
-> hash[hash of email can be derived form email]

photos[as there could be multiple photoes for single user so multiple rows would be there for same userId]
-> id
-> userId
-> isActive
//if we build gravitar and take image from user this is the same photos table that we want to create entry in image upload service after pushing image to s3 via presigned url


query for getting a photos -- if we don't store hash in users table
select * from photos join users on users.userId = userId 
where hash(users.email) = inputHash

inputhash is provided as input, whats bad in this query is as we are doing compuatation on email each time to get the hash so EVAL has to each time run through all the rows to know
which matches inputHash that we can optimise by just storing hash and then indexing on it to save on time

optimised query
select * from photos join users on users.userId = userId 
where users.hash = inputHash


query for setting a image as active
update photos set isActive = true
where id = inputImageId

whats bad in this query
-> anybody can update others active image user id should be passed in input systematically
-> more than one image can be set to active

update photos set isActive = false
where userId = inputUserId and isActive = true

update photos set isActive = true
where id = inputImageId and userId = inputUserId and isActive = false

still one problem is there suppose first query succeed then there is a failure so it leads to inconsistency so better enclose it in TRANSACTION block.

query for getting active photoes

select * from photos join users on users.userId = userId 
where users.hash = inputHash and isActive = true

to optimise it we have to think a bit out of the box as we are reading all the rows for a particular user 
as in input we are getting userId not the photoId so to just have the active photo we have to store active photo against user.
so new schema should be 

users
-> userId
-> email
-> hash[hash of email can be derived form email]
-> active_photo_id

photos
-> id
-> userId

now for the update query also we need not to have 2 queries and first set all photos of user as false then update one as true instead we can just set the active_photo_id

whole design (1:10:00)
user -> photoupload service -> upload to S3 via presigned url
     -> call to API to push photoid in photos DB

   API -> update in DB
       -> push a event in kafka for CDN Invalidation so that resent image can be served 

user2 -> gravitar/<hashid> : gravitar is a CDN with origin API server -> gets active_photo_id fro DB creates S3 path gets the image 

FYI: frontend provide CDN url with BE server origin

ondemand image processing
<cdn>/hash?imagePixel=23

so generally this is handled by cdns no as they provide it out of the box but if they not we get the exact params at API server and we do image processing with liberary like
imageMagik and return the image to scale only way is to get machines with high hardware capabilities because job is compute intensive and also scale horizontally.

Impact of having a functionality where people can tag other people in a photo
-> who can tag me
-> max limit of people tagged in a photo
-> self removal of my tag
-> notification
-> face recognition and suggestion of tags
-> users activity page (DB should support query of where all this user is tagged)
-> we need to store the tag cordinates relatively so if photo size change on diff device it is still file


SD: for newly unread messages
the icon should show the count of people that messaged which are unread and on click of icon count should reset

Requirements
-> near realtime
-> update on new message received from user

first we design the DB schema
messages:
msg
from 
to
timestamp
we might index composite basis: to, timestamp, from

user:
userId
last_read_at

query to find the count 
select count(unique(from)) from messages join user on user.userId = to
where to = inputUserID and timestamp > user.last_read_at

Now this would just work fine but we just want the count so we can optimise it

like we can have a key value pair

userId: Set<>{unique from user messages}. [redis can be used]

now everytime a message come if the message is comming from a user that is already in set the count would stay the same if user is new it increases
and on user read or just click or messages icon the set can be cleared

so every syetem can be built in ways one is directly calling the source of truth the messages table that gets automatically updated as messages come or other we can optimise and 
have data in a more consumable form to support the functionality which can dec the latency but increase the maintainability as messages would be added in messages table
but to support this functionality we would also write logic this way and get the count instantly.

final design
so for user to do message that goes in dB and also update the count of unique user unsent message [message that are already delivered means user is active on messaging seen we don't 
want to update the count] we need a messaging service that might use that online/ofline indicator and all to figure out
user2 -> on websocket connection -> messaging service -> message DB
                                                     -> on unsent message -> kafka -> consumer {identify from[user2] and to[user1] of message} -> Auxilary redis
                                                                                                                                 -> if on Aux redis count inc ->  main redis 

get path user1 -> service gets the count -> main redis 
user1 -> service clears the count for user1 -> main redis
                                            -> Aux redis

now as main redis is already under load with having get and update + same users does a lot of messages why we want to check his uniqueness on main redis we can do it on Aux
then if count inc we can do it on main one even if it goes down we can have fallback to do it directly on main redis then, we are aware that on write path there would be another hop
but the cost is not much as we are preventing redis(DB) to be under load as most messages won't change it's state

DB is most brittle component if opp is not changing it's state we should avoid it. 

improvement functionality: if there are already unique 100+ user messages we can just reject writes on DB and show in frontend 100+
for Aux redis instead of that set we can have a diff schema like user1_user2 {to_from} exist so don't update if doesn't exist add in aux and udate main redis

Storage Engine

CDCs

suppose company relies on database mongoDB and want to put its data into SQL DB easy way is to push the events into a Kafka queue and then process them and push the data in SQL
- but that is not a good way as data won't be consistent as through events we can't capture all the data as everything doesn't have event.
- and like in event you can get the whole data that you want to write to target but then network IO would be huge and if you get the Id for which data can be fetched from
source DB by the consumers and written to target but still we are loosing for having batched reads and writes.

SO better is we do through cdc [change data capture] tools like airbyte, dbezium
CDC directly captures data from database and pushes that into target database with doing certain transformations
now capturing data from database can be in different forms like reading WAL , BIN_LOG files and applying changes to target but it depends upon type of database

CDC have there own small db where they take remote locks on a table know which table is read untill which point so that more than one CDC can do the work on diff table in parallel.

the pervious systems that we build where we got the data and from db and passed on that we can also do through cdcs as they are more reliable and fast also as they pull raw large 
amount data and also we can configure CDC no replica to avoid load. eg MY sql -> cdc -> elastic search or even S3 if we want.

Note: one CDC can write to multiple targets.

Distributed In memory cache

first on single node cache:
-> storage
most probably we would need a key value pair so hashmap can be used

now the key would be just string, but the value could be anything so suppose like in java we define value to be OBject but while accessing how would be know that
so for value we should have a structure like
{
   value -> value Object pointer or void*
   type: type of value to which it should be parsed// we keep in 4 bits only because there are not many types and in-momory we want to utilise space efficienctly
   lru_bits: 28 bits// for eviction
}
now why we need pointer because we might want to have this stucture of fixed size so that it can be better stored sequentially if in list.

now flow would be suppose value is set for our key and we want to add we can just do 'add/push' first it would check if type of our key supports that opp then do else throw error.

-> Cache Full
but now we would want to cap the memory usage because we don't want to allocate more if memory is already full suppose our cache just reserved 1 GB and if we go out of it we get 
SEGMENTAION_FAULT
so as whenever we allocate memory we would be using some function like malloc so we can have a wrapper around it
redis -> zmalloc(type, count){
   if(totalsize+type*count>max){evict}
   o = malloc(type,count)
   totalsize+=tye*count
   return o;
}

-> Eviction

Now for eviction we can keep lru/lfu timstamp/frequency in the same value object

general implemenation of lru: for each key value pair we make a node in doubly linked list as we access them we can pop it out and move it to head and when we want to evict 
we can evict from tail. [each node location is in hashmap]
But we are designing a cache suppose key value pair is total of 5 bytes and for making this node
{
   prevptr
   afterptr
   val
}
this takes total 12 bytes its a waste of space so better we can have a randome eviction redis does provide lru/lfu options but they are higly optimised.
might cause most imp key deleted from cache but would save a lot of space.

When we want to use 
LFU: least frequently used -> when we know this key is popular in past may not be getting a lot of hits now because of some external reason but in future same trend would be there 
so we don't want to evict this key, example wikipidia pages suppose new thing came people started searching about it but the standard key shouldn't be evicted because that new thing 
might not be hot topic in future but that standard high frequecy read article will get searches.

LRU: Least recently used: when there is some trend that is popular now but won't be popular in future example reel trend or news article.

-> communication
Now there are various protocols like http, grpc
but like http where we would be sending data with in specified format that would increase the payload size also and 3 way handshake can be inefficient
in grpc the connection is open but data goes in compressed form so high amout of data transfer can be served through this
or we can define our own protocol over tcp so any which way our usecase suits we can use.

-> ttl
as we discussed previously how redis handles this
we can store ttl in that same value object
by lazy deletion and that sampling of 20 key is > 25% are expired resample

it is far more efficient than making a priority queue for the keys and all accoring to ttl which would not be space and time optimised.

-> cuncurrency

we can have atomic variables , we can have locking implemented at thread level via mutex and semaphores

optimistic locking
when we are optimistic that we don't require locking so for that we need opps that if 2 opps comes same time only one of them would succeed
like update a = 30 where a =10
a = 20 where a=10
so if we can do something that at a time only one of them would run then we won't require locking 
so we have compare and swap that is a operation provided by CPU for if that is runing OS won't do context switching it is provided by every language//attomic instructions
-- read more about it

single threaded
if there are not multiple thread won't cause concurrency :)

Distributed cache(HLD)
Now as load grows or we need more storage we would need more nodes so we can distribute the load

Now we can make mutually exclusive sets like keys from [a,j] goes to 1 node [k,q] on second node and rest on last 

-> routing
there are primiarly 4 ways to do this
-> having a routing layer in between that tells which node to go: have a extra hop
-> client is smart knows which node to go: if topology changes client needs to be updated
-> client just connects to one node that node connects to other seeding node and gets the response like bittorrent: complex for a cache
-> client asks a node and it returns the IP of node that should be connected for the required key that's how redis does this atleast for write where if request goes on replica it
sends IP of master

2:07
-> scaling
if we do a hash based routing [that we generally do] 
serverToWhichKeyroute=hashfun(key)%NoOfServer
suppose we want to add/remove node that will cause a lot of data transfer mostly 50-60% of keys would need reshuffleing to other nodes

to solve this consistant hashing is the thing that should be used.[MINIMAL DATA TRANSFER]
server would be in ring(2^128 big) what ever hashfun(IP of server) returns where it sits in ring,hashfun(key) just the sever next to the right is the owner of that key.

only the keys that lies before the removed node and last node to it are needed data transfer--in case of removal 
-> if we want to remove a node gracefully the data of node that is going to be removed just needs to be shifted to node afterwards.
-> if we want to add node gracefully we add the data of node afterwards to it then slowly reove that data from node afterwards

if suppose removal happen abruptly due to node down then max what happens key would start checking on next node to the one removed it doesn't have data so there would be
cache miss and data is retrieved from BE server so might inc a bit of load there -- same for direct addition of node.

now suppose if we add virtual node means more than 1 place for a node in ring it would just make more data transfer in case of node addition/removal which defeats the purpose
of consistant hashing a bit, but it's still not that bad if we have a usecase.

Distributed cache are nothing but DHT distrubuted hash tables that are optimised for minimal data transfer using techniques like consistant hashing
everything on internet is DHT just change the storage 
in memory - distributed cache
on storage - distributed storage

like Distributed cache there is a version that connect to nearest server it will tell you where to connect to similar for DHT bittorant does the same.

-> Availability
have replicas or standby nodes
if using consistant hashing next node can have the data of previous one or may be 2 next nodes[replication factor]

-> reliability
write ahead log whenever you write something it goes in this file when cache rebuilds/reboots it can build using that. redis uses this.

Making a english dictionary 
keys - meanings (read heavy)
- we have to store 1TB data for like 170000 words
- we can't use any existing Data base
- some latency is fine
- writes for addition or updation are only once a week so we can have a big process for that

-> storage
if we store in json then as to search we need to load the whole json in memory 1Tb data -- expensive
better is we store in a text file like a CSV

-> querying
key:meaning\n
now for reading doing a linear search means again loading that 1TB file in memory which we don't want, so when reads are slow we should have indexing
index-> key,offset(integer representing row of key in csv)\n
size of index avg size of key -> 4.3 in eng dictionary
4.3+4(Int)+2(, and \n)=10.3 -> 10.3*170000(words) = 1.75 mb which is peanuts for cpu

so index size is very small we can load it in memory every time and have a pointed read in main csv file

-> protability means how easy we can ship
so Ideally we should join these and ship together
How to identify that here index would end we can add some special char for that but that is a bit tricky hack
better is on start we store the byte offset where the index ends
this is like the header of our file
like different file formats png have first 3 bytes saying p n g
so that decoder can verify the format
so our headers would be 
- my_db[some random name] 5Byte
- version 4Byte
- index_offset [4Byte] value -> 10.3*170000

So when application boots it has decoder for our format it knows reading 13 Byte for headers then extract the index and load a hashtable in memory then start retrieving data,
Thats how majorly all file formats are built, parquet format is also like this having headers and all
50:00


Now lets talk about infra
user-> LB-> API servers -> shared storage

now we can attach a SSD with each API server whats bad with that
-> cost: storing 1TB on each server is not fine
-> small problem is could lead to some inconsistancy when updates fail on any server

So we are having a shared storage 
what we want from it to provide us random reads or the file system symentics like we give (file, offset, n[bytes from offset]) and it should give us that only
This can be done by any network attach storage OR DB since we are not using DB here so any blob storage could work like S3 or HDFS

So API server will first pull 16 bytes header than read the index bytes and form a in memory hash table of index than ready for a pointed read. Thats a small key value store.

Now lets look at write path
suppose lets say as we get the change log file once a week we simply go to that index and replace the words meaning, that we can do in ram but on disk as we update
it will eventually start overwriding the existing bytes and might overwride the next word.

so better is we create a new file As the dictionary would be sorted and change log is also sorted if not we can sort change log ourselves then do a merge sort type thing
have a pointer on each the file data and change log, move the file data pointer as the word is matching with change log add the new defiantion in new file and move the 
change log pointer. 
But if we do it on network that would be very much slow as it is a huge file so we should spin up a new machine for writes and have a requirement to have big disk in it.
load the data in disk and create a new file in disk then lets deploy.

Now what we can do is deploy on same path but the problem with that is 
suppose file is currupted or upload failed and we remove the existing file we have to do a lot to restore that + have downtime
and major problem is we want new indexes as file is new so we immidiately have to inform the API servers to again create indexes


so better is we upload to a new path but each week updating a path mannualy + if the update extends, is not a good approach
lets have a meta file in S3 which have a fixed path that server can pull to check the latest path

but server will eaither have to poll the meta file which seems unnecessary so better we tell the server to get it so as the upload is done by new machine we can pass a event in
pub/sub which servers are subscribed to then they know they have to read new file and create index with it.
but isn't that complex + that is what server does on restart 

so we just need a new deployement so as the file is uploaded we update the meta and trigger a new deployment maybe a jenkins pipeline to reboot/restart the API servers

so we made a ability to do pointed reads over S3 thats what S3 Athena does, its used for moving our hot data stored in ssds to cold storage where we can still do pointed reads

PS: To get the performance we don't directly store data in S3 in simple format we store it in apache hudi to make more retrievable and athena or prestoDB retrive that through
Hive queries which internally uses spark jobs to process them and get data.

1:46

Superfast DB kv
ram,  ssd,       hdd
harddrive storage are cheap and because data is storead on actual magnetic disk, how data is retrieved first sector is located disk is rotated and head moves to that location
then actual read happens, so with this cheap storage we want to create a KV.

Everything with persistance
put
we can just have a file and do append only operations so that once the head is at the sector where file is present we can just move it ahead and save the searching time.
delete
we can again append the key with value some special char that represents deletion.
update
for updation we can append the new value again
get
we can have a in memory hash that points to ofset in disk and on delete we can remove index , on update point to diff location and if machine restarts we just have to make
index again.

limitaions 
-> we can fit in as many keys as we can fit in allocate memory for ram
-> and we are not collecting garbage so there we are limited by values we can store at disk.

so what we should do is as the file grows , as we know the file system also doesn't allows for very big files + we want to filter out garbage without effecting the fast writes
that we are suppoting via append only thing, so we can split the files like decide a certain threshold suppose 500mb then start writing to other file, now that file we are writing
is active all other are just read only files this is called file rotation. Now we can do merge and compact on the non active files to get the garbage collected.
we can read those files and make a new files out of them without the deleted values and delete the old files but now we have to do the in memory index updation also.
index would be like Key-> file, offset

now we have to take care about the file rotation threshold and at the same time the merge and compact frequency or flush frequency so that our cpu is not loosing on 
performacy due to doing merge and compact a lot of times + our disk won't get full with stale data, so we need to adjust these values and make the system running smooth.

Now on storing this applies to dictionary also
we don't just store in csv or some file directly because how would we read a key value then [command: read untill \n] that is very unoptimised as it involves a lot of disk I/O's

so we should store in blocks like K:bytes,val:bytes-> then actual key value
so when reading we first get the total bytes then read key value

now suppose while writing a gigantic value system crashed and after one block second block got stored so while reading those no. of total bytes we get on to reading partial bytes
of other block that is really a problem of serving currupt data that we can avoid, How?
by just taking hash of K:byte,Vbytes,K,V called CRC [cyclic redundancy check] and storing at the front so that it along with K:bytes and V: bytes would be stored in first, 
as this we want to be stored atomic disk I/O so the even if we failed to store next bytes we can again compute if hash is correct and then only serve the data.
so block is like CRC:Timestamp:Kbyte,Vbytes,K,V
index would be like Key-> fileId,total record size,offset,timestamp

so what we designed is bitcask  
its a liberary that we can run on server that provide high throughput for storing and retrieving data, Uber uses Riak that is a wrapper over bitcask that make it distributed
like elastic search is a wrapper over lucy similar 

These are called log structured storages:
kafka is a example of it as well, whenever we need high write throughput log structured storages or append only files are the solution, each topic in kafka has n partitians, assume
partitians as nothing but append only files only.

multi-tiered storage
when the data we are storing is huge even after having read replicas and sharding we can't able to support it or we see that most of data is infrequently accessed so better to move 
that data some where else not on main database.

Hot tear 
transactional 
reads/writes happen
like mysql
consistant
expensive compared to other
fast

warm tear
non transactional 
reads happen
horizonatlly scalable
little slower

cold tear
in frequent reads 
for compliance purpose
should be cheap

multitier storage design
-> suppose we have a order service that is queried by payments, user, logistics , and the writes happen to ordersDB which is transactional [Hot Storage]
-> the data here has some deletion policy 
-> A cron job runs periodically which transfers the data from HOT tier to a staging storage [temporary Dump] and not only the data from ordersDB but it kind of agreegates the data 
   from different sources like payments and logistics and structures it into a big document specific to a orderID.
-> from staging storage (S3) a spark job[loader] picks the data and transfers it to warm tier which is horizontally scallable and distributed like may be in MongoDB, Elastic search, 
   Dynamo DB, HDFS
-> now this tier is basically read only.
-> Now how our service figures which tier to query that's where our Id generation logic comes if we have snowflake with timestamp in it and we know our data deletion policy we can
   know where to find the data.
-> similar way we can transfer data from warm to cold storage like S3 and store it in a structured way like apache Hodie, if we need pointed reads we can have that pointed reads
   via athena(dictionary like that we made perviously) or prestoDB but as it would be slow, if we want to fire a lot of queries on it for a usecase we can have a job that transfers
   specific data to a temporary DB where we can fire queries and have analytics on it.
-> BTW we can use CDC's while transfering the data as we just care about data and not dealing with application logic.

Just some facts
-> we can turn on and of for bin logs 
-> bin logs are generally stored on disk and have a retention policy configured
-> if CDC's/replicas consuming those bin logs crashed when they get back and start consuming from where they left and at that point suppose bin log gets deleted and we want that data
for replica that is a situation which is like reforming the replica where we take a snapshot of primary data and from that point start applying logs on it, similarly for CDC we 
can have a strategy.
-> how does CDC and replica know where they left of when they get crashed CDC generally have external storage to store that mainly that orchestrator of CDC has access to which tell
particaly instance, and replica generally store this meta info in disk where the data exist to which it has applied logs onto certain point.
-> suppose the source of CDC is down that is why bin logs are accessed by CDC to consume and suppose target is down so better strategy would be putting events into kafka that remains
persistant for a while that target consumer can pick and applies.
-> relay logs generally replica also have relay logs that are logs received from primary they get purged once processed they act as a buffer untill the replica applies them.
-> why having CDC on replica doesn't mean no cost, because then replica has to enable bin logs atleast which might not be needed elsewhere.

1: 00:00

lets Build S3 like storage

storage:
on S3 everything is key value pair the paths where we want to store the data example /temp/bin/abc.txt this is a key and the file is the value for it to S3 we always provide the 
memory our file would consume like 1 GB and path.
on normal file system the paths and all are managed by filesystem itself so under a directory path is a inode that has its meta data like size location on disk like its sector 
it's a log structure storage only but the paths are managed and inodes are like a tree structure.
S3 is also log structure because we want to use HDD as they are cheap and instead of having inodes we simply have key value where key is path.

so suppose 1 disk gets filled by data that we put in it then we would go to next disk and then to next so generally they are structured together in linked list type structure
and managed under storage rack by drivers and in data center we have multiple storage rack connected together.

suppose 1 disk is 10 TB and there are 20-30 disk in rack and we decide to give 1 rack to each customer but suppose customer just fill 100GB and rest is waste, so to lower our cost
we want to fill the rest.

As we know S3 supports editing a file or deleting a file, now as we know to have write throughput we have to do it in append only way, we need to make new file for new edits and 
mark the files as non active for deletes[remove entry from external DB].

to know which file version is located where on disk like the path that driver of rack provide S3 stores that in a seperate DB {bucket:disk,key-> sector, version of file}[another 
DB for pointed reads like that hash in server in dictionary example]

and at merge and compaction we will remove all this data, BTW S3 also provide a feature of object versioning if we enable it s3 don't delete the previous versions during 
merge and compaction.

How driver move to next disk?
As we don't want to impact write throughput so we start the process of moving to next disk as disk is 70% full, so still intermidiately we would be writing on old disk. and 30%
buffer that we have we can use in merge and compaction also. 
1:50

Rounting:
user -> S3 servers -> storage racks

now how to decide which rack to route to suppose we have these items to store
bucket1:Key1, bucket1:Key2, bucket2:Key1, bucket2:Key2, bucket3:Key1
and we decide we hash bucket and key and assign some rack to that hash but to very hot keys got assigned same hash and even we control the request per min at api server level
[throtelling] per bucket, still if the request comes for same rack in worst case so that's why hash based routing is not feasible here due to hot partitian problem. As we don't
want the control of routing to go to hash function.

so in this case we generally prefer range based partitianing like [a,g] [h,l] [m,z] suppose [h,l] is hot we can split it, the control lies with us and side effect that is has
is tenant isolaction suppose some company netflix have there data mainly in one rack or nearer rack.

Solution for solving hot partitian problem[not related to routing]
-> As shards are logical seperations only every db provide command to take a dump of it, so suppose some partitians get hot we can take dump of some and move that out to diff. 
server and remove existing one, changing the configuration of location of shard for which this partiatian is.

-> Elastic search provide this out of the box it provide, with head plugin we just need to drag and drop the hot partitian to another node.

Now as there are multiple storage racks S3 servers can't determine which rack to call as for a product like S3 there would be huge amount of racks, and all the different request 
from netflix, prime goes to S3 api server which also make that hot so better is we assign this job of routing to rack for another set of partitian servers that knows which rack
contains data for a particular partiatian
user -> s3 servers -> partitian servers -> storage rack
so one partitian is owned by 1 partitan server but one partitan server can own multiple partitians
partitain server[a,e] -> node 1[a,b](rack)
                      -> node 2[c,e]
so both the racks and partiatian server handle request in range based partitians and server know which rack to access.
In case one partitian server gets hot we can transfer some partitians that it owns to another server, this is done by partitian manager or orchestrator. 
For ensuring availability of partitian manager we can have leader elections like workers will check the partitian server health checks and if worker goes down taken care by 
master and if master goes down taken care by leader election.
so s3 servers check partitain map table managed by orchestrator and call to partitan server that talks to rack.

Here the question is why we are directly checking partitian map as it is managed by orchestrator or partitian manager shouldn't we call partitian manager it gets the partitan for
us and then fwd the request to that partitian and directly gives us data.
Because here we are dealing with huge amount of data we want less hops in data transfer. so we directly talk to partiatian server itself. and we know partitian map is nearly static
won't change much so going through that manager layer is better here. This is how we go counter-intuitive and build in more practical way.

Durability:
only way to insure is redundancy
be it writing in same disk in 2 sectors
in multiple disk in same rack
in multiple racks
in multiple dcs of same region[async]
in multiple regions[async]
due to this in previous layer a lot of additional info needs to be stored

integrity
passing checksum at every layer and checking before passing the data be it reading or writing.

S3 chunking part
as S3 stores in log structure storage which lets say can't reserve huge tbs of data for a file before hand but S3 gurantees that they can store lets say 2 tb[logical final] so 
chunking would happen into 2000 1 gb files[physical seperation] and all the chunks metadata would be stored in hash table like chunk1 in this sector and chunk 2 in this etc.
So As S3 is write heavy system we can get a huge file from user and suppose our network and disk write at certain speed we can parallelize writes into chunks,
we can build to read/write in chunks but generaly we do sequential so it's not very much needed.

High throughput system 2
so As we have designed bitcask which is optimal on reads due to hash in ram and write due to append only writes with minimum head movement

but as we are persisting each write in every request we are reducing a some of throughput, we can accept write and have it in buffer[ram] and later store in disk.
this is how we can have a impact in performance but what if our server goes down, yup if we are okay with loosing on a bit of durability 
As this high write system suits purpose for sensors producing events, frontend like moving of cursor and all analytics of that anything that is produced by machine
google maps streaming person location,
so if that goes down for 1 min that won't have a big impact right so we could live with that,
but writing on ram would inc the infra cost - if we estimate that how much it would increase the cost so the graph would be like a tooth-saw first inc then on flushing to disk
then there would be down trend[ram utilisation graph]
if my ram is 1000mb and we are writing at 1mbps then we should flush every 1000sec

How the reads look like 
simple ans is we first check the ram if we get then return else we check in storage check if it exist and returns else 404, it does look like ram but we are not seting value
from disk to ram if it is a mis as that would inc the infra cost that we don't want, here we have to do multi tier reads.

now as we are writing we have some key value hash map in ram that we are flushing to disk may be in some header and key value structure similar to bitcask so what format we should 
write -- ss[string-sorted] tables it's a format that serializes that hash in ram to key value in disk.

Now when we write eaither we can append in that same file or create a new file, appending in same file holds the complexity if we make the file too big it would slower the
write and there is some limitation on file system level also. and when we append we have to update the indexes of the file so that they are correct according to new new values.
if it is time series database with sequential writes and as key would be time so would be just moving forwad then we can have just one file which would be better as done by
influx DB.

so lets say we create the new file always and one thing to remember we are not having that fast lookup for keys in ram as we have in bitcask because we are building a write 
heavy system and we are handeling a lot of file updatign hash is something we don't want to accomodate, we just have ss table in ram that we periodically flush
so if we have a lot of files we have to iterate through all of them to know if a value exist and if it does all the previos values are stale so we should also have merge
and compaction to remove that, and as the ss table provide the new entries in sorted format only so we just have to do merge of sorted values that we have done previously
which is a lite opperation.
so how disk utilisation looks like tooth saw but with each tooth  hight increases and the depth starting point also goes up as we are writing some new data every time.
and on merge and compaction we would see some dip.

but if a team has mostly lookups where key doesn't exist that is worst for them, so we should know at ram level we should search or not.
so for that we can use bloom filters [they get the hash and turn up the key] as when they say no it is 100% key don't exist and when they say yes it means key may exist.
if we store in set we need to store every bit of key which would take the space in RAM. and bloom filters take constant space and in ram we don't want to keep things that can 
grow exponentially.
and suppose we want to store this set or bloom filter on disk even then doing a disk based set lookup would be very much expensive than a bloom filter based lookup because
bloom filter is just a array.// we have to store this in disk as we have to make it persistant because on reboot of machine we don't want to make it again so eaither we can lookup
in disk or load that in memory.

suppose some team doesn't want to loose any of there data, so that would be problem for us
one possible solution could be we can write everything in a WAL file too on disk and as we flush our in memory buffer to disk we will remove that WAL file, so even if our system
crash we can reconstruct using that WAL file, and provide this functionality as a configuration as it would reduce the write throughput.
but wouldn't bit cask a better solution for them.
-> could be but they are bounded by number of keys in ram here we are bounded by no. of keys in disk which can be huge as we are flushing the ss in disk periodically but if the team doesn't have 
high no. of keys than bitcask is a way to go for them.
-> And WAL would just be the file that has recent changes and to reboot the system we just want the recent changes to make our in memory ss table so there will be faster reboots
and high availability. as in bit cask have to read whole file to make in memory hash lookup.
-> And as we have recent key value both in ram we can just serve that if request comes for them.

So this was LSM tree used by -> rocksDB, levelDB, BadgerDB
-> when we need high write throughput and want faster recent added reads LSM trees shine there.
LSM tree -> tree is as we create new file in disk every time when we merge and compact it reduces to less files[level 2] then less[level3] that forms a tree structure.

why not use redis instead as it also provide persistance functionality -> as it provide persistance but still everything should still fill in ram so it should be memory bound. 

1: 50
youtube video processing pipeline
-> video upload - same insta photo upload to direct storage(S3) via presigned url
-> video processing 
-> video distribution

-> video processing once the video is uploaded before publishing
   we want to transcode it into some format like 480p 
   generate thumbnail
   check for objectionable content
after publishing
   we can generate more types of formats like 720p and 320p
   generate transcripts
   recommendation

so when can user hit that publish button - once all the 3 conditions are met 
so may be we can put a message in kafka after the video is uploaded and the consumers can pick it up but we can trigger the publish once all three are done, for that we might 
have to write some custom logic taking care of retries and all which would add a lot of complexity 
instead we can use a workflow management tool like - apache airflow. which abstarcts all that and provide a visual represnetation of nodes to design our workflow.
underlying there is a master which is the orchestrator that puts task in queue and any worker can pick that and update the result in db. master polls DB and check which nodes
are completed and just pushes there children in queue the periodically runs a job that check whoever in queue does they have any other dependancy if not put in main execution 
queue from where workers can pick.


transcoding
formats are containers that can have video, audio subtitles, stored on disk
these are decoded by codecs 

generally ffmpeg is used for transcoding: once we upload video youtube takes that runs ffmpeg for different formats and upload them

for live interactive streams: janus, webrtc, TURN servers
resources: 100ms, howvideo.works

for live matches they are actually not live 
as the footage is captured it is send in some segments at a freqency those segments are transcoded at the time and when we pull that through cdn, cdn defaults to s3 as origin 
and we get the transcoded format in which we are watching if we change the format the app like hotstar would pass that format in params to cdn it will fetch that format particular
segment and provide to use then it constantly fetches 1 segment after another.

whole flow
-> user upload to video upload service which creates a entry in video db for video id and provide presigned url where we can upload the video
-> while video is being uploaded video service also calls channel service to create entry in channel db which holds info for meta data of video as we don't want to couple it
together because we want to send them seperate so that video playing won't get delayed, as on youtube if we click may be info comes late but video starts running, so both
calls can come parallely. 
-> once video upload is done it calls the workflow management Airflow through which a workflow is triggered and video made ready to be published. and data in channelDb can also be 
populated by user like title, description, status(by default draft)
-> once video is published we can do the distribution- we can push events to a kafka via cdc of our channel db 
-> now the search service can consume the event to create index for elastic search, we can capture watchtime and push event to kafka which can be used by trending service, which 
can call CDN decider to ask CDN to direcly cache that trending video.
                                          
